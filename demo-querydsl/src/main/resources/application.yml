spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/spb1?characterEncoding=utf8
    driverClassName: com.mysql.jdbc.Driver
    username: root
    password: lead
  jpa:
      database: mysql
      show-sql: true
      hibernate:
        ddl-auto: create-drop
